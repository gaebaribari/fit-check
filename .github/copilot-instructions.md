# Project Guidelines

This repository is currently empty. As code is added, these instructions should be updated with concrete details.

## Code Style
- Follow the language-specific idioms of whatever language is being introduced.
- Use a linter/formatter (e.g. `eslint`, `prettier`, `black`, etc.) once a language is selected.

## Architecture
- N/A yet. When the first components are added, document the major modules, service boundaries, and data flows here.

## Build and Test
- No build or test commands exist yet. Add instructions below when tests or build tools are added:
  ```bash
  # install dependencies
  # build project
  # run tests
  ```
- Agents should attempt `npm install` / `pip install -r requirements.txt` if those files appear.

## Project Conventions
- No special conventions are defined yet.  Update this section once patterns emerge.

## Integration Points
- None currently.  Record any external APIs, databases, or services the project depends on.

## Security
- No authentication or secrets managed yet.  Once sensitive areas exist, document the patterns used for handling credentials, tokens, etc.

---

> ⚠️ **Note:** This is a placeholder file created automatically.  Please expand each section with real information as the project evolves.
